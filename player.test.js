const player = require('./player');

test('players can take turns attacking each other', ()=>{
    expect(player()).toBe()
    const spy = jest.spyOn(/**some player Object */,/**a method from the player object being spyed on*/);

    //all ship should not be sunk
    const /**a return value from a method call*/ = playersGameBoard./*a method being called*/();

    expect(spy).toHaveBeenCalled();
      
    expect(/**the variable holding the return value from the method call*/).toStrictEqual(/**some expectation*/);
});